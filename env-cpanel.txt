# DCS Risk Register - cPanel Production Environment
# Copy this file to .env on your cPanel hosting

# Application Configuration
APP_NAME="DCS Risk Register"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://register.dcs.com.na
APP_TIMEZONE=Africa/Windhoek

# Database Configuration (cPanel MySQL)
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=
DB_SOCKET=

# Cache Configuration (File-based for cPanel)
CACHE_DRIVER=file
CACHE_PREFIX=dcs_risk_register_cache_

# Session Configuration (File-based for cPanel)
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_SECURE_COOKIES=true
SESSION_HTTP_ONLY=true
SESSION_SAME_SITE=lax

# Queue Configuration (Sync for cPanel - no Redis needed)
QUEUE_CONNECTION=sync

# Mail Configuration (cPanel SMTP)
MAIL_MAILER=smtp
MAIL_HOST=localhost
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@dcs.com.na
MAIL_FROM_NAME="${APP_NAME}"

# Logging Configuration
LOG_CHANNEL=stack
LOG_LEVEL=info
LOG_DEPRECATIONS_CHANNEL=null

# Performance Configuration
DASHBOARD_CACHE_DURATION=300
DASHBOARD_CACHE_ENABLED=true
RISK_MATRIX_CACHE_DURATION=300
RISK_MATRIX_CACHE_ENABLED=true
CLIENT_STATS_CACHE_DURATION=600
CLIENT_STATS_CACHE_ENABLED=true

# Database Performance
DB_QUERY_TIMEOUT=30
DB_SLOW_QUERY_THRESHOLD=0.5
DB_ENABLE_QUERY_LOGGING=false
DB_CONNECTION_POOL_SIZE=10

# Frontend Performance
FRONTEND_AUTO_REFRESH=300000
FRONTEND_LAZY_LOADING=true
FRONTEND_IMAGE_OPTIMIZATION=true
FRONTEND_MINIFY_ASSETS=true

# Monitoring & Analytics
PERFORMANCE_TRACKING=true
MEMORY_USAGE_THRESHOLD=50
CACHE_EFFICIENCY_TRACKING=true
SLOW_QUERY_LOGGING=true

# API Configuration
API_RATE_LIMITING=true
API_RATE_LIMIT=60
API_RESPONSE_CACHING=true
API_CACHE_TTL=300
API_COMPRESSION=true
API_GZIP_LEVEL=6

# Search Configuration
FULLTEXT_SEARCH_ENABLED=true
FULLTEXT_MIN_WORD_LENGTH=3
ELASTICSEARCH_ENABLED=false
ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200

# File Upload Configuration
IMAGE_COMPRESSION_ENABLED=true
IMAGE_COMPRESSION_QUALITY=85
IMAGE_MAX_WIDTH=1920
IMAGE_MAX_HEIGHT=1080
FILE_MAX_SIZE=10240
FILE_ALLOWED_TYPES=pdf,doc,docx,xls,xlsx,jpg,jpeg,png

# Security Configuration
BCRYPT_ROUNDS=12

# Broadcasting Configuration
BROADCAST_DRIVER=log
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_CLUSTER=mt1

# Additional Services (Optional)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# External APIs (Optional)
GOOGLE_ANALYTICS_ID=
SLACK_WEBHOOK_URL=
